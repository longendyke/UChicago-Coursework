public enum Type {
    NAT, BOOL
}

/* You can refer to these values as Type.NAT and Type.BOOL anywhere else in the code. */
/* You can compare Type objects with .equals, as in "if (tau.equals(Type.NAT)) ..."   */
